interface PostWalletType {
  name: string;
  description?: string;
  initial_balance: number;
}

interface WalletType {
  id: string;
  name: string;
  description?: string;
  initial_balance: number;
}
